import type { Apartment, SearchParams } from '../types.js';

export async function scrapeFourzida(params: SearchParams): Promise<Apartment[]> {
  // Placeholder for future implementation
  return [];
}
